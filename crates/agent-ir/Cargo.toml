[package]
name = "agent-ir"
description = "Agent IR: a compilation infrastructure for agentic systems. Facade crate and `agent-ir` command line driver."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
authors.workspace = true
default-run = "agent-ir"

[dependencies]
agent-ir-core = { workspace = true }
agent-ir-dialects = { workspace = true }
agent-ir-parser = { workspace = true }
agent-ir-verifier = { workspace = true }
agent-ir-analysis = { workspace = true }
agent-ir-passes = { workspace = true }
agent-ir-lowering = { workspace = true }
agent-ir-runtime = { workspace = true }
clap = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }

[[bin]]
name = "agent-ir"
path = "src/main.rs"
