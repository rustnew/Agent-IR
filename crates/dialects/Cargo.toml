[package]
name = "agent-ir-dialects"
description = "Dialect definitions and the operation registry for Agent IR (core, agent, control, tool, memory, observation)."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
authors.workspace = true

[dependencies]
agent-ir-core = { workspace = true }
