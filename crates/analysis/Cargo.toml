[package]
name = "agent-ir-analysis"
description = "Dataflow, dominance, dependency-graph and effect analyses over Agent IR."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
authors.workspace = true

[dependencies]
agent-ir-core = { workspace = true }
agent-ir-dialects = { workspace = true }

[dev-dependencies]
agent-ir-parser = { workspace = true }
